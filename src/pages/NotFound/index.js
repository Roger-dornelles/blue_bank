
import { Container } from './styled';

const NotFound = ()=>{

    return(
        <Container>
            <h2>Opss, Desculpe não encontramos o que você procura...</h2>
        </Container>
    )
};

export default NotFound;