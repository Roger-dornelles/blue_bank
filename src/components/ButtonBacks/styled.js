import styled from 'styled-components';

export const Button = styled.button`

        a{
        position: absolute;
        top: 50px;
        left: 100px;
        color:rgba(205, 10, 30);
        font-size:2.2em;
    }
`